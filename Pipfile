[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
torch = ">=2.0.0"
torchvision = ">=0.15.0"
transformers = ">=4.30.0"
tokenizers = ">=0.13.0"
librosa = ">=0.10.0"
soundfile = ">=0.12.0"
pillow = ">=9.5.0"
streamlit = ">=1.25.0"
matplotlib = ">=3.7.0"
seaborn = ">=0.12.0"
numpy = ">=1.24.0"
pandas = ">=2.0.0"
python-dotenv = ">=1.0.0"
huggingface-hub = ">=0.16.0"

[dev-packages]

[requires]
python_version = "3.12"
